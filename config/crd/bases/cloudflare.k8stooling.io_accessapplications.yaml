---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: accessapplications.cloudflare.k8stooling.io
spec:
  group: cloudflare.k8stooling.io
  names:
    kind: AccessApplication
    listKind: AccessApplicationList
    plural: accessapplications
    singular: accessapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessApplication is the Schema for the accessapplications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            type: object
            properties:
              tenant:
                type: string
              secret_ref:
                type: string
                description: "Reference to the Cloudflare API secret. Format: 'secret-name' or 'namespace/secret-name'."
              domain:
                type: string
              destinations:
                type: array
                items:
                  type: object
                  properties:
                    type: { type: string }
                    uri: { type: string }
                    zone_name: { type: string }
              session_duration:
                type: string
                description: "How long a user session is valid (e.g., '24h', '15m')."
                default: "24h"
              allowed_idps:
                type: array
                description: "List of Identity Provider (IdP) UUIDs permitted to authenticate."
                items:
                  type: string
              policies:
                type: array
                description: "Access policies defining authentication rules and exceptions."
                items:
                  type: object
                  properties:
                    name:
                      type: string
                    decision:
                      type: string
                      enum: [allow, deny, bypass, non_identity]
                    precedence:
                      type: integer
                    include:
                      type: array
                      items:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    exclude:
                      type: array
                      items:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    require:
                      type: array
                      items:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                  required:
                    - name
                    - decision
                    - precedence
            required:
              - tenant
              - secret_ref
              - domain
              - destinations
              - allowed_idps
              - policies
            x-kubernetes-preserve-unknown-fields: true
          status:
            description: Status defines the observed state of AccessApplication
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
